[project]
name = "auth-service"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.138.0",
    "passlib>=1.7.4",
    "pydantic[email]>=2.13.4",
    "python-jose>=3.5.0",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true }
