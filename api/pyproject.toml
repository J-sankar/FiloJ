[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.29.0",
    "shared",
    "python-multipart>=0.0.32",
    "python-magic>=0.4.27",
]

[tool.uv.sources]
shared = { workspace = true }
