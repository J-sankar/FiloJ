[project]
name = "image-processor"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "piexif>=1.1.3",
    "pillow>=12.2.0",
    "shared",
]

[tool.uv.sources]
shared = { workspace = true }
